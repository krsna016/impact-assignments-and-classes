-- CREATE DATABASE database_1
-- (In order to not get any error we can use 'if not exists')
CREATE DATABASE if NOT EXISTS database_1;
-- Creating other database:
CREATE DATABASE if NOT EXISTS database_2;


    -- DEFAULT CHARACTER SET = 'utf8mb4'; It's all okay if we not write this line